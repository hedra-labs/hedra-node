// This file was auto-generated by Fern from our API Definition.

import * as Hedra from "./api/index.js";
import type { BaseClientOptions, BaseRequestOptions } from "./BaseClient.js";
import { type NormalizedClientOptionsWithAuth, normalizeClientOptionsWithAuth } from "./BaseClient.js";
import { mergeHeaders, mergeOnlyDefinedHeaders } from "./core/headers.js";
import * as core from "./core/index.js";
import { toJson } from "./core/json.js";
import * as environments from "./environments.js";
import { handleNonStatusCodeError } from "./errors/handleNonStatusCodeError.js";
import * as errors from "./errors/index.js";

export declare namespace HedraClient {
    export type Options = BaseClientOptions;

    export interface RequestOptions extends BaseRequestOptions {}
}

export class HedraClient {
    protected readonly _options: NormalizedClientOptionsWithAuth<HedraClient.Options>;

    constructor(options: HedraClient.Options = {}) {
        this._options = normalizeClientOptionsWithAuth(options);
    }

    /**
     * @param {Hedra.ListModelsRequest} request
     * @param {HedraClient.RequestOptions} requestOptions - Request-specific configuration.
     *
     * @throws {@link Hedra.UnprocessableEntityError}
     *
     * @example
     *     await client.listModels()
     */
    public listModels(
        request: Hedra.ListModelsRequest = {},
        requestOptions?: HedraClient.RequestOptions,
    ): core.HttpResponsePromise<Hedra.AiModel[]> {
        return core.HttpResponsePromise.fromPromise(this.__listModels(request, requestOptions));
    }

    private async __listModels(
        request: Hedra.ListModelsRequest = {},
        requestOptions?: HedraClient.RequestOptions,
    ): Promise<core.WithRawResponse<Hedra.AiModel[]>> {
        const { types } = request;
        const _queryParams: Record<string, unknown> = {
            types: types !== undefined ? toJson(types) : undefined,
        };
        const _authRequest: core.AuthRequest = await this._options.authProvider.getAuthRequest();
        const _headers: core.Fetcher.Args["headers"] = mergeHeaders(
            _authRequest.headers,
            this._options?.headers,
            requestOptions?.headers,
        );
        const _response = await core.fetcher({
            url: core.url.join(
                (await core.Supplier.get(this._options.baseUrl)) ??
                    (await core.Supplier.get(this._options.environment)) ??
                    environments.HedraEnvironment.Default,
                "models",
            ),
            method: "GET",
            headers: _headers,
            queryString: core.url
                .queryBuilder()
                .addMany(_queryParams)
                .mergeAdditional(requestOptions?.queryParams)
                .build(),
            timeoutMs: (requestOptions?.timeoutInSeconds ?? this._options?.timeoutInSeconds ?? 60) * 1000,
            maxRetries: requestOptions?.maxRetries ?? this._options?.maxRetries,
            abortSignal: requestOptions?.abortSignal,
            fetchFn: this._options?.fetch,
            logging: this._options.logging,
        });
        if (_response.ok) {
            return { data: _response.body as Hedra.AiModel[], rawResponse: _response.rawResponse };
        }

        if (_response.error.reason === "status-code") {
            switch (_response.error.statusCode) {
                case 422:
                    throw new Hedra.UnprocessableEntityError(
                        _response.error.body as Hedra.HttpValidationError,
                        _response.rawResponse,
                    );
                default:
                    throw new errors.HedraError({
                        statusCode: _response.error.statusCode,
                        body: _response.error.body,
                        rawResponse: _response.rawResponse,
                    });
            }
        }

        return handleNonStatusCodeError(_response.error, _response.rawResponse, "GET", "/models");
    }

    /**
     * @param {HedraClient.RequestOptions} requestOptions - Request-specific configuration.
     *
     * @example
     *     await client.listVoices()
     */
    public listVoices(requestOptions?: HedraClient.RequestOptions): core.HttpResponsePromise<Hedra.Asset[]> {
        return core.HttpResponsePromise.fromPromise(this.__listVoices(requestOptions));
    }

    private async __listVoices(
        requestOptions?: HedraClient.RequestOptions,
    ): Promise<core.WithRawResponse<Hedra.Asset[]>> {
        const _authRequest: core.AuthRequest = await this._options.authProvider.getAuthRequest();
        const _headers: core.Fetcher.Args["headers"] = mergeHeaders(
            _authRequest.headers,
            this._options?.headers,
            requestOptions?.headers,
        );
        const _response = await core.fetcher({
            url: core.url.join(
                (await core.Supplier.get(this._options.baseUrl)) ??
                    (await core.Supplier.get(this._options.environment)) ??
                    environments.HedraEnvironment.Default,
                "voices",
            ),
            method: "GET",
            headers: _headers,
            queryString: core.url.queryBuilder().mergeAdditional(requestOptions?.queryParams).build(),
            timeoutMs: (requestOptions?.timeoutInSeconds ?? this._options?.timeoutInSeconds ?? 60) * 1000,
            maxRetries: requestOptions?.maxRetries ?? this._options?.maxRetries,
            abortSignal: requestOptions?.abortSignal,
            fetchFn: this._options?.fetch,
            logging: this._options.logging,
        });
        if (_response.ok) {
            return { data: _response.body as Hedra.Asset[], rawResponse: _response.rawResponse };
        }

        if (_response.error.reason === "status-code") {
            throw new errors.HedraError({
                statusCode: _response.error.statusCode,
                body: _response.error.body,
                rawResponse: _response.rawResponse,
            });
        }

        return handleNonStatusCodeError(_response.error, _response.rawResponse, "GET", "/voices");
    }

    /**
     * @param {Hedra.ListAssetsRequest} request
     * @param {HedraClient.RequestOptions} requestOptions - Request-specific configuration.
     *
     * @throws {@link Hedra.UnprocessableEntityError}
     *
     * @example
     *     await client.listAssets({
     *         type: "text"
     *     })
     */
    public listAssets(
        request: Hedra.ListAssetsRequest,
        requestOptions?: HedraClient.RequestOptions,
    ): core.HttpResponsePromise<Hedra.Asset[]> {
        return core.HttpResponsePromise.fromPromise(this.__listAssets(request, requestOptions));
    }

    private async __listAssets(
        request: Hedra.ListAssetsRequest,
        requestOptions?: HedraClient.RequestOptions,
    ): Promise<core.WithRawResponse<Hedra.Asset[]>> {
        const { type: type_, ids } = request;
        const _queryParams: Record<string, unknown> = {
            type: type_,
            ids,
        };
        const _authRequest: core.AuthRequest = await this._options.authProvider.getAuthRequest();
        const _headers: core.Fetcher.Args["headers"] = mergeHeaders(
            _authRequest.headers,
            this._options?.headers,
            requestOptions?.headers,
        );
        const _response = await core.fetcher({
            url: core.url.join(
                (await core.Supplier.get(this._options.baseUrl)) ??
                    (await core.Supplier.get(this._options.environment)) ??
                    environments.HedraEnvironment.Default,
                "assets",
            ),
            method: "GET",
            headers: _headers,
            queryString: core.url
                .queryBuilder()
                .addMany(_queryParams)
                .mergeAdditional(requestOptions?.queryParams)
                .build(),
            timeoutMs: (requestOptions?.timeoutInSeconds ?? this._options?.timeoutInSeconds ?? 60) * 1000,
            maxRetries: requestOptions?.maxRetries ?? this._options?.maxRetries,
            abortSignal: requestOptions?.abortSignal,
            fetchFn: this._options?.fetch,
            logging: this._options.logging,
        });
        if (_response.ok) {
            return { data: _response.body as Hedra.Asset[], rawResponse: _response.rawResponse };
        }

        if (_response.error.reason === "status-code") {
            switch (_response.error.statusCode) {
                case 422:
                    throw new Hedra.UnprocessableEntityError(
                        _response.error.body as Hedra.HttpValidationError,
                        _response.rawResponse,
                    );
                default:
                    throw new errors.HedraError({
                        statusCode: _response.error.statusCode,
                        body: _response.error.body,
                        rawResponse: _response.rawResponse,
                    });
            }
        }

        return handleNonStatusCodeError(_response.error, _response.rawResponse, "GET", "/assets");
    }

    /**
     * @param {Hedra.CreateAssetRequest} request
     * @param {HedraClient.RequestOptions} requestOptions - Request-specific configuration.
     *
     * @throws {@link Hedra.UnprocessableEntityError}
     *
     * @example
     *     await client.createAsset({
     *         name: "name",
     *         type: "text"
     *     })
     */
    public createAsset(
        request: Hedra.CreateAssetRequest,
        requestOptions?: HedraClient.RequestOptions,
    ): core.HttpResponsePromise<Hedra.CreateAssetResponse> {
        return core.HttpResponsePromise.fromPromise(this.__createAsset(request, requestOptions));
    }

    private async __createAsset(
        request: Hedra.CreateAssetRequest,
        requestOptions?: HedraClient.RequestOptions,
    ): Promise<core.WithRawResponse<Hedra.CreateAssetResponse>> {
        const _authRequest: core.AuthRequest = await this._options.authProvider.getAuthRequest();
        const _headers: core.Fetcher.Args["headers"] = mergeHeaders(
            _authRequest.headers,
            this._options?.headers,
            requestOptions?.headers,
        );
        const _response = await core.fetcher({
            url: core.url.join(
                (await core.Supplier.get(this._options.baseUrl)) ??
                    (await core.Supplier.get(this._options.environment)) ??
                    environments.HedraEnvironment.Default,
                "assets",
            ),
            method: "POST",
            headers: _headers,
            contentType: "application/json",
            queryString: core.url.queryBuilder().mergeAdditional(requestOptions?.queryParams).build(),
            requestType: "json",
            body: request,
            timeoutMs: (requestOptions?.timeoutInSeconds ?? this._options?.timeoutInSeconds ?? 60) * 1000,
            maxRetries: requestOptions?.maxRetries ?? this._options?.maxRetries,
            abortSignal: requestOptions?.abortSignal,
            fetchFn: this._options?.fetch,
            logging: this._options.logging,
        });
        if (_response.ok) {
            return { data: _response.body as Hedra.CreateAssetResponse, rawResponse: _response.rawResponse };
        }

        if (_response.error.reason === "status-code") {
            switch (_response.error.statusCode) {
                case 422:
                    throw new Hedra.UnprocessableEntityError(
                        _response.error.body as Hedra.HttpValidationError,
                        _response.rawResponse,
                    );
                default:
                    throw new errors.HedraError({
                        statusCode: _response.error.statusCode,
                        body: _response.error.body,
                        rawResponse: _response.rawResponse,
                    });
            }
        }

        return handleNonStatusCodeError(_response.error, _response.rawResponse, "POST", "/assets");
    }

    /**
     * @param {Hedra.BodyUploadAssetPublicAssetsIdUploadPost} request
     * @param {HedraClient.RequestOptions} requestOptions - Request-specific configuration.
     *
     * @throws {@link Hedra.UnprocessableEntityError}
     *
     * @example
     *     import { createReadStream } from "fs";
     *     await client.uploadAsset({
     *         file: fs.createReadStream("/path/to/your/file"),
     *         id: "id"
     *     })
     */
    public uploadAsset(
        request: Hedra.BodyUploadAssetPublicAssetsIdUploadPost,
        requestOptions?: HedraClient.RequestOptions,
    ): core.HttpResponsePromise<Hedra.Asset> {
        return core.HttpResponsePromise.fromPromise(this.__uploadAsset(request, requestOptions));
    }

    private async __uploadAsset(
        request: Hedra.BodyUploadAssetPublicAssetsIdUploadPost,
        requestOptions?: HedraClient.RequestOptions,
    ): Promise<core.WithRawResponse<Hedra.Asset>> {
        const _body = await core.newFormData();
        await _body.appendFile("file", request.file);
        const _maybeEncodedRequest = await _body.getRequest();
        const _authRequest: core.AuthRequest = await this._options.authProvider.getAuthRequest();
        const _headers: core.Fetcher.Args["headers"] = mergeHeaders(
            _authRequest.headers,
            this._options?.headers,
            mergeOnlyDefinedHeaders({ ..._maybeEncodedRequest.headers }),
            requestOptions?.headers,
        );
        const _response = await core.fetcher({
            url: core.url.join(
                (await core.Supplier.get(this._options.baseUrl)) ??
                    (await core.Supplier.get(this._options.environment)) ??
                    environments.HedraEnvironment.Default,
                `assets/${core.url.encodePathParam(request.id)}/upload`,
            ),
            method: "POST",
            headers: _headers,
            queryString: core.url.queryBuilder().mergeAdditional(requestOptions?.queryParams).build(),
            requestType: "file",
            duplex: _maybeEncodedRequest.duplex,
            body: _maybeEncodedRequest.body,
            timeoutMs: (requestOptions?.timeoutInSeconds ?? this._options?.timeoutInSeconds ?? 60) * 1000,
            maxRetries: requestOptions?.maxRetries ?? this._options?.maxRetries,
            abortSignal: requestOptions?.abortSignal,
            fetchFn: this._options?.fetch,
            logging: this._options.logging,
        });
        if (_response.ok) {
            return { data: _response.body as Hedra.Asset, rawResponse: _response.rawResponse };
        }

        if (_response.error.reason === "status-code") {
            switch (_response.error.statusCode) {
                case 422:
                    throw new Hedra.UnprocessableEntityError(
                        _response.error.body as Hedra.HttpValidationError,
                        _response.rawResponse,
                    );
                default:
                    throw new errors.HedraError({
                        statusCode: _response.error.statusCode,
                        body: _response.error.body,
                        rawResponse: _response.rawResponse,
                    });
            }
        }

        return handleNonStatusCodeError(_response.error, _response.rawResponse, "POST", "/assets/{id}/upload");
    }

    /**
     * @param {Hedra.ListGenerationsRequest} request
     * @param {HedraClient.RequestOptions} requestOptions - Request-specific configuration.
     *
     * @throws {@link Hedra.UnprocessableEntityError}
     *
     * @example
     *     await client.listGenerations()
     */
    public listGenerations(
        request: Hedra.ListGenerationsRequest = {},
        requestOptions?: HedraClient.RequestOptions,
    ): core.HttpResponsePromise<Hedra.PagedResponseGeneration> {
        return core.HttpResponsePromise.fromPromise(this.__listGenerations(request, requestOptions));
    }

    private async __listGenerations(
        request: Hedra.ListGenerationsRequest = {},
        requestOptions?: HedraClient.RequestOptions,
    ): Promise<core.WithRawResponse<Hedra.PagedResponseGeneration>> {
        const {
            type: type_,
            created_before: createdBefore,
            created_after: createdAfter,
            prompt_query: promptQuery,
            agent_thread_id: agentThreadId,
            ids,
            limit,
            offset,
        } = request;
        const _queryParams: Record<string, unknown> = {
            type: Array.isArray(type_)
                ? type_.map((item) => (typeof item === "string" ? item : toJson(item)))
                : type_ !== undefined
                  ? typeof type_ === "string"
                      ? type_
                      : toJson(type_)
                  : undefined,
            created_before: createdBefore !== undefined ? createdBefore : undefined,
            created_after: createdAfter !== undefined ? createdAfter : undefined,
            prompt_query: promptQuery,
            agent_thread_id: agentThreadId,
            ids,
            limit,
            offset,
        };
        const _authRequest: core.AuthRequest = await this._options.authProvider.getAuthRequest();
        const _headers: core.Fetcher.Args["headers"] = mergeHeaders(
            _authRequest.headers,
            this._options?.headers,
            requestOptions?.headers,
        );
        const _response = await core.fetcher({
            url: core.url.join(
                (await core.Supplier.get(this._options.baseUrl)) ??
                    (await core.Supplier.get(this._options.environment)) ??
                    environments.HedraEnvironment.Default,
                "generations",
            ),
            method: "GET",
            headers: _headers,
            queryString: core.url
                .queryBuilder()
                .addMany(_queryParams)
                .mergeAdditional(requestOptions?.queryParams)
                .build(),
            timeoutMs: (requestOptions?.timeoutInSeconds ?? this._options?.timeoutInSeconds ?? 60) * 1000,
            maxRetries: requestOptions?.maxRetries ?? this._options?.maxRetries,
            abortSignal: requestOptions?.abortSignal,
            fetchFn: this._options?.fetch,
            logging: this._options.logging,
        });
        if (_response.ok) {
            return { data: _response.body as Hedra.PagedResponseGeneration, rawResponse: _response.rawResponse };
        }

        if (_response.error.reason === "status-code") {
            switch (_response.error.statusCode) {
                case 422:
                    throw new Hedra.UnprocessableEntityError(
                        _response.error.body as Hedra.HttpValidationError,
                        _response.rawResponse,
                    );
                default:
                    throw new errors.HedraError({
                        statusCode: _response.error.statusCode,
                        body: _response.error.body,
                        rawResponse: _response.rawResponse,
                    });
            }
        }

        return handleNonStatusCodeError(_response.error, _response.rawResponse, "GET", "/generations");
    }

    /**
     * @param {Hedra.GenerateAssetRequest} request
     * @param {HedraClient.RequestOptions} requestOptions - Request-specific configuration.
     *
     * @throws {@link Hedra.UnprocessableEntityError}
     *
     * @example
     *     await client.generateAsset({
     *         type: "video",
     *         generated_video_inputs: {
     *             text_prompt: "text_prompt"
     *         }
     *     })
     */
    public generateAsset(
        request: Hedra.GenerateAssetRequest,
        requestOptions?: HedraClient.RequestOptions,
    ): core.HttpResponsePromise<Hedra.GenerateAssetResponse> {
        return core.HttpResponsePromise.fromPromise(this.__generateAsset(request, requestOptions));
    }

    private async __generateAsset(
        request: Hedra.GenerateAssetRequest,
        requestOptions?: HedraClient.RequestOptions,
    ): Promise<core.WithRawResponse<Hedra.GenerateAssetResponse>> {
        const _authRequest: core.AuthRequest = await this._options.authProvider.getAuthRequest();
        const _headers: core.Fetcher.Args["headers"] = mergeHeaders(
            _authRequest.headers,
            this._options?.headers,
            requestOptions?.headers,
        );
        const _response = await core.fetcher({
            url: core.url.join(
                (await core.Supplier.get(this._options.baseUrl)) ??
                    (await core.Supplier.get(this._options.environment)) ??
                    environments.HedraEnvironment.Default,
                "generations",
            ),
            method: "POST",
            headers: _headers,
            contentType: "application/json",
            queryString: core.url.queryBuilder().mergeAdditional(requestOptions?.queryParams).build(),
            requestType: "json",
            body: request,
            timeoutMs: (requestOptions?.timeoutInSeconds ?? this._options?.timeoutInSeconds ?? 60) * 1000,
            maxRetries: requestOptions?.maxRetries ?? this._options?.maxRetries,
            abortSignal: requestOptions?.abortSignal,
            fetchFn: this._options?.fetch,
            logging: this._options.logging,
        });
        if (_response.ok) {
            return { data: _response.body as Hedra.GenerateAssetResponse, rawResponse: _response.rawResponse };
        }

        if (_response.error.reason === "status-code") {
            switch (_response.error.statusCode) {
                case 422:
                    throw new Hedra.UnprocessableEntityError(
                        _response.error.body as Hedra.HttpValidationError,
                        _response.rawResponse,
                    );
                default:
                    throw new errors.HedraError({
                        statusCode: _response.error.statusCode,
                        body: _response.error.body,
                        rawResponse: _response.rawResponse,
                    });
            }
        }

        return handleNonStatusCodeError(_response.error, _response.rawResponse, "POST", "/generations");
    }

    /**
     * @param {Hedra.GetStatusRequest} request
     * @param {HedraClient.RequestOptions} requestOptions - Request-specific configuration.
     *
     * @throws {@link Hedra.UnprocessableEntityError}
     *
     * @example
     *     await client.getStatus({
     *         generation_id: "generation_id"
     *     })
     */
    public getStatus(
        request: Hedra.GetStatusRequest,
        requestOptions?: HedraClient.RequestOptions,
    ): core.HttpResponsePromise<Hedra.GenerationStatusResponse> {
        return core.HttpResponsePromise.fromPromise(this.__getStatus(request, requestOptions));
    }

    private async __getStatus(
        request: Hedra.GetStatusRequest,
        requestOptions?: HedraClient.RequestOptions,
    ): Promise<core.WithRawResponse<Hedra.GenerationStatusResponse>> {
        const { generation_id: generationId } = request;
        const _authRequest: core.AuthRequest = await this._options.authProvider.getAuthRequest();
        const _headers: core.Fetcher.Args["headers"] = mergeHeaders(
            _authRequest.headers,
            this._options?.headers,
            requestOptions?.headers,
        );
        const _response = await core.fetcher({
            url: core.url.join(
                (await core.Supplier.get(this._options.baseUrl)) ??
                    (await core.Supplier.get(this._options.environment)) ??
                    environments.HedraEnvironment.Default,
                `generations/${core.url.encodePathParam(generationId)}/status`,
            ),
            method: "GET",
            headers: _headers,
            queryString: core.url.queryBuilder().mergeAdditional(requestOptions?.queryParams).build(),
            timeoutMs: (requestOptions?.timeoutInSeconds ?? this._options?.timeoutInSeconds ?? 60) * 1000,
            maxRetries: requestOptions?.maxRetries ?? this._options?.maxRetries,
            abortSignal: requestOptions?.abortSignal,
            fetchFn: this._options?.fetch,
            logging: this._options.logging,
        });
        if (_response.ok) {
            return { data: _response.body as Hedra.GenerationStatusResponse, rawResponse: _response.rawResponse };
        }

        if (_response.error.reason === "status-code") {
            switch (_response.error.statusCode) {
                case 422:
                    throw new Hedra.UnprocessableEntityError(
                        _response.error.body as Hedra.HttpValidationError,
                        _response.rawResponse,
                    );
                default:
                    throw new errors.HedraError({
                        statusCode: _response.error.statusCode,
                        body: _response.error.body,
                        rawResponse: _response.rawResponse,
                    });
            }
        }

        return handleNonStatusCodeError(
            _response.error,
            _response.rawResponse,
            "GET",
            "/generations/{generation_id}/status",
        );
    }

    /**
     * @param {HedraClient.RequestOptions} requestOptions - Request-specific configuration.
     *
     * @example
     *     await client.getCredits()
     */
    public getCredits(requestOptions?: HedraClient.RequestOptions): core.HttpResponsePromise<Hedra.CreditBalance> {
        return core.HttpResponsePromise.fromPromise(this.__getCredits(requestOptions));
    }

    private async __getCredits(
        requestOptions?: HedraClient.RequestOptions,
    ): Promise<core.WithRawResponse<Hedra.CreditBalance>> {
        const _authRequest: core.AuthRequest = await this._options.authProvider.getAuthRequest();
        const _headers: core.Fetcher.Args["headers"] = mergeHeaders(
            _authRequest.headers,
            this._options?.headers,
            requestOptions?.headers,
        );
        const _response = await core.fetcher({
            url: core.url.join(
                (await core.Supplier.get(this._options.baseUrl)) ??
                    (await core.Supplier.get(this._options.environment)) ??
                    environments.HedraEnvironment.Default,
                "billing/credits",
            ),
            method: "GET",
            headers: _headers,
            queryString: core.url.queryBuilder().mergeAdditional(requestOptions?.queryParams).build(),
            timeoutMs: (requestOptions?.timeoutInSeconds ?? this._options?.timeoutInSeconds ?? 60) * 1000,
            maxRetries: requestOptions?.maxRetries ?? this._options?.maxRetries,
            abortSignal: requestOptions?.abortSignal,
            fetchFn: this._options?.fetch,
            logging: this._options.logging,
        });
        if (_response.ok) {
            return { data: _response.body as Hedra.CreditBalance, rawResponse: _response.rawResponse };
        }

        if (_response.error.reason === "status-code") {
            throw new errors.HedraError({
                statusCode: _response.error.statusCode,
                body: _response.error.body,
                rawResponse: _response.rawResponse,
            });
        }

        return handleNonStatusCodeError(_response.error, _response.rawResponse, "GET", "/billing/credits");
    }

    /**
     * Make a passthrough request using the SDK's configured auth, retry, logging, etc.
     * This is useful for making requests to endpoints not yet supported in the SDK.
     * The input can be a URL string, URL object, or Request object. Relative paths are resolved against the configured base URL.
     *
     * @param {Request | string | URL} input - The URL, path, or Request object.
     * @param {RequestInit} init - Standard fetch RequestInit options.
     * @param {core.PassthroughRequest.RequestOptions} requestOptions - Per-request overrides (timeout, retries, headers, abort signal).
     * @returns {Promise<Response>} A standard Response object.
     */
    public async fetch(
        input: Request | string | URL,
        init?: RequestInit,
        requestOptions?: core.PassthroughRequest.RequestOptions,
    ): Promise<Response> {
        return core.makePassthroughRequest(
            input,
            init,
            {
                baseUrl: this._options.baseUrl ?? this._options.environment,
                headers: this._options.headers,
                timeoutInSeconds: this._options.timeoutInSeconds,
                maxRetries: this._options.maxRetries,
                fetch: this._options.fetch,
                logging: this._options.logging,
                getAuthHeaders: async () => (await this._options.authProvider.getAuthRequest()).headers,
            },
            requestOptions,
        );
    }
}
