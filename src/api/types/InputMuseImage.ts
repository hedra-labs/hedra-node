// This file was auto-generated by Fern from our API Definition.

import type * as Hedra from "../index.js";

/**
 * Model-specific inputs for `muse-image`.
 *
 * Accepted field combinations (one per input mode):
 * (1) requires: aspect_ratio, images, prompt
 * (2) requires: aspect_ratio, prompt; must omit: images
 */
export interface InputMuseImage {
    /** Generation prompt. */
    prompt: string;
    /** Number of outputs generated per job. Only 1 is supported. */
    num_outputs?: number | undefined;
    /** Rewrite the prompt before generation. An LLM expands it into a fuller description and the model receives that text instead of the submitted one; the result's `prompt` reports what ran. */
    enhance_prompt?: boolean | undefined;
    /** Output aspect ratio. */
    aspect_ratio: InputMuseImage.AspectRatio;
    /** Images to edit or blend. 1 to 10 images, each at most 30 MB. */
    images?: Hedra.InputMuseImageImagesItem[] | undefined;
    /** Output image format. */
    output_format?: InputMuseImage.OutputFormat | undefined;
}

export namespace InputMuseImage {
    /** Output aspect ratio. */
    export const AspectRatio = {
        Adaptive: "adaptive",
        TwentyOne9: "21:9",
        Sixteen9: "16:9",
        Three2: "3:2",
        Four3: "4:3",
        One1: "1:1",
        Three4: "3:4",
        Two3: "2:3",
        Nine16: "9:16",
        Nine21: "9:21",
    } as const;
    export type AspectRatio = (typeof AspectRatio)[keyof typeof AspectRatio];
    /** Output image format. */
    export const OutputFormat = {
        Jpeg: "jpeg",
        Png: "png",
        Webp: "webp",
    } as const;
    export type OutputFormat = (typeof OutputFormat)[keyof typeof OutputFormat];
}
