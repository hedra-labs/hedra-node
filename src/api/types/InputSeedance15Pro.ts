// This file was auto-generated by Fern from our API Definition.

import type * as Hedra from "../index.js";

/**
 * Model-specific inputs for `seedance-15-pro`.
 */
export interface InputSeedance15Pro {
    /** Number of outputs generated per job. Only 1 is supported. */
    num_outputs?: number | undefined;
    /** Generation prompt. */
    prompt: string;
    /** Output aspect ratio. */
    aspect_ratio: InputSeedance15Pro.AspectRatio;
    /** Output resolution. */
    resolution: InputSeedance15Pro.Resolution;
    /** Duration in ms. */
    duration_ms: number;
    /** Whether to generate native audio for the video. */
    generate_audio?: boolean | undefined;
    /** Start frame (image-to-video). From 300px to 6000px on each side, with an aspect ratio from 0.4 to 2.5, and at most 30 MB. */
    start_image?: Hedra.InputSeedance15ProStartImage | undefined;
    /** End frame (first-last-frame-to-video). From 300px to 6000px on each side, with an aspect ratio from 0.4 to 2.5, and at most 30 MB. */
    end_image?: Hedra.InputSeedance15ProEndImage | undefined;
    /** Seed for reproducible output; omit for a random seed. From 0 to 4294967295. */
    seed?: number | undefined;
}

export namespace InputSeedance15Pro {
    /** Output aspect ratio. */
    export const AspectRatio = {
        One1: "1:1",
        Three4: "3:4",
        Four3: "4:3",
        Sixteen9: "16:9",
        TwentyOne9: "21:9",
        Nine16: "9:16",
    } as const;
    export type AspectRatio = (typeof AspectRatio)[keyof typeof AspectRatio];
    /** Output resolution. */
    export const Resolution = {
        FourHundredEightyP: "480p",
        SevenHundredTwentyP: "720p",
        OneThousandEightyP: "1080p",
    } as const;
    export type Resolution = (typeof Resolution)[keyof typeof Resolution];
}
